function table (){

    for (let first=1; first <=10; first++){
        for (let sec=1; sec<=10; sec++){
            console.log(`${first} * ${sec} = ${first*sec}`);
        }
    }

}
table ();
